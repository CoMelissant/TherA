# Core inference
torch>=2.0.0
torchvision>=0.15.0

# Pinned mutually-compatible stack.
# The LLaVA-1.5 codebase (haotian-liu style) predates the v5 / "heterogeneity"
# config system introduced in transformers 4.54+. Using an unbounded
# transformers>=4.36 installs a v5-era build whose nested config + new
# attention attrs (attention_dropout, rope_parameters, ...) break custom
# LlavaConfig and the old flat llava-1.5-7b-hf config.json.
# Pin the whole huggingface stack + diffusers to a consistent early-2024 set.
transformers==4.36.2
tokenizers>=0.14.1
huggingface-hub==0.20.3
diffusers==0.25.1
accelerate==0.25.0
safetensors==0.4.1

# Image I/O
pillow>=9.0.0
tqdm>=4.65.0

# LLaVA on-the-fly mode only (optional for reference-cache inference)
peft==0.7.1
sentencepiece>=0.1.99
protobuf>=3.20.0

# LLaVA vision backbone
timm>=0.9.0
einops>=0.7.0
